name: ci

on:
  push:
  pull_request:

# One run per ref. A superseded push has nothing to prove.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify:
    uses: ./.github/workflows/build.yml
