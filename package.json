{
  "name": "nginx-njs-antibot",
  "version": "0.0.1",
  "description": "Proof-of-work bot gate for nginx, implemented as an njs module with a SIMD WebAssembly solver",
  "license": "BlueOak-1.0.0 OR Apache-2.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "./build.sh",
    "test": "node test/solver.test.mjs && node test/module.test.mjs && node test/module-fuzz.mjs"
  },
  "engines": {
    "node": ">=25"
  }
}
